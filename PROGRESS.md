# PROGRESS

- **Project**: mcp-apple-reminders (python-mcp; macOS-only EventKit + ReminderKit MCP server)
- **Active branch**: `main` (trunk strategy)
- **Active feature spec**: `specs/002-modernize-and-foundation/`
- **Active TASK_STATE**: `TASK_STATE.md`
- **Last session**: 2026-05-28 (Claude — autonomous slice run; **31 slices shipped**)

## 🎯 Where we are

**Phase 0 (substrate)** — ✅ COMPLETE — S0.1 → S0.6.
**Phase 1 (P0 capabilities)** — ✅ COMPLETE — S1.0 → S1.8.
**Phase 2 (MCP primitives)** — ✅ COMPLETE — S2.1 (Resources), S2.2 (Prompts), S2.3 (progress), S2.4 (elicitation), S2.5 (sampling).
**Phase 3 (feature parity)** — ✅ COMPLETE — S3.1 (time alarms), S3.2 (location alarms), S3.3 (recurrence), S3.4 (bulk ops), S3.5 (multi-cal query), S3.6 (completed-in-range).
**Phase 4 (visibility-plane + cross-cutting)** — ✅ COMPLETE (4 of 5; S4.2 stretch deferred) — S4.1 (visibility-plane pilot), S4.3 (streamable HTTP opt-in), S4.4 (security review), S4.5 (docs sweep).
**Stretch (S4.2 TodoWrite mirror)** — deferred for a future iteration where Claude Code's TodoWrite hook surface becomes hookable.

## Quick stats

- **37 MCP tools** registered (live, post-S3.4 + S4.1).
- **3 static Resources + 2 templated** (`reminders://` family + `agents://current/{project_name}`).
- **4 Prompts** (`daily_review`, `weekly_retro`, `brain_dump_triage`, `agent_visibility_sync`).
- **71 source files** scanned by the architecture gate; every file under the 400-LOC hard cap; module-shape gate green across 41 countable modules.
- **`make lint && make check-architecture`** — green.
- **7 live round-trips PASSED** this session against the user's real Reminders.app:
  - `create_calendar`
  - `create + rename + delete`
  - `subtasks` (parent + 3 children + cleanup)
  - `set_flagged` (flip on/off)
  - `tags` (add 2 + SQLite filter assertion)
  - `assign_section` (create section + assign + verify)
  - `set_alarm` (1h relative) + `set_location_alarm` (SF City Hall) + `set_recurrence` (weekly/[1,3,5])

## Auto-generated artifacts

- **`docs/TOOLS.md`** — live tool / resource / template / prompt catalog. Regenerated by `./venv/bin/python -c '... mcp.list_tools() ...'`.
- **`docs/SQLITE_SCHEMA.md`** — schema notes for the Reminders.app CoreData store + the UUID-equivalence contract + the concurrency lesson from S1.5 (`immutable=1` cache bug).
- **`docs/SECURITY-REVIEW.md`** — OWASP MCP Top 10 walk-through against this surface (S4.4).
- **`src/mcp_apple_reminders/_native/THIRD_PARTY_NOTICES.md`** — vendored upstream (RemCTL MIT) source attribution + pin.

## Last three decisions (newest first)

- 2026-05-28 — `set_parent` (subtask reparenting) deferred to a follow-up that extends the borrowed Obj-C helper with a `set_parent` action. Documented in S1.5 changelog.
- 2026-05-28 — `set_tags` is **additive only** (Obj-C `add_tags` action doesn't remove). Parameter is named `add_tags=` to be honest. Replacement waits on `clear_tags` helper extension.
- 2026-05-28 — SQLite `connect()` opens with `mode=ro` **without** `immutable=1`. The latter caches file contents and prevented the reader from seeing concurrent helper writes — fix discovered mid-S1.5 and is what made every live round-trip green.

## Open follow-ups (NOT blockers)

- README.md user-facing rewrite (the project README is still a "what this is" pointer; the auto-generated `docs/TOOLS.md` is the authoritative surface).
- `set_parent` helper extension (subtask reparenting).
- `clear_tags` helper extension (so `set_tags` can be replacement rather than additive).
- `tools/_kill_switch.py` helper + `VIBE.yaml::agents.tool_flags` integration (S4.4 documented the YAML sketch but didn't ship the helper).
- S4.2 TodoWrite mirror — pending hook surface in Claude Code.
- `Reminder.alarms` + `Reminder.recurrence` denormalization on the SQLite read side (write side ships in S3.1/3.2/3.3).

## How to resume

1. Read `AGENTS.md` (project contract, gotchas — including the new §10.4 session-start visibility-plane rule).
2. Read `TASK_STATE.md` (current phase + slice; all phases marked done).
3. Skim `docs/TOOLS.md` for the current capability surface.
4. Spec changes require an ADR — there is no current first-class change planned, but the open follow-ups above are good candidates.
5. Run `./venv/bin/python verify_setup.py` to confirm the environment is intact before touching code.

## Do NOT

- Edit `src/mcp_apple_reminders/models.py` after S0.3 — the field order is the contract freeze point for the rest of the spec.
- Add new `exclude_globs` to `VIBE.yaml::architecture` without explicit user approval. The borrowed Swift + Obj-C sources at `_native/src/*` are the one approved exception (documented in `_native/THIRD_PARTY_NOTICES.md`).
- Touch the `Claude-*` workflow lists (Pierce's pre-existing ADHD task system). The new visibility-plane lives under `Agents-<project>`.
- Bypass commit signing (`-S` is mandatory) or skip the per-commit VERSION bump.
- Re-open `immutable=1` on the SQLite connection — it would re-break concurrent reads after helper writes. Documented in `docs/SQLITE_SCHEMA.md`.
- Reach for AppleScript bridging — the chosen path is the borrowed RemCTL Swift + Obj-C helpers.
