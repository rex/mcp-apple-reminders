
# Agent caches and logs (agentic-skeleton PR1)
# Distribution
# IDE
# Local configuration (if any)
# Logs
# MCP/tool caches
# MyPy
# Python
# Ruff
# Terraform agent artifacts
# Testing
# Virtual Environment
*$py.class
*.egg
*.egg-info/
*.log
*.py[cod]
*.so
*.swo
*.swp
*.tfplan
*.whl
*~
.DS_Store
.Python
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v*
.claude/logs/
.claude/serena-initialized
.claude/session-context.md
.claude/settings.local.json
.context7-cache/
.coverage
.dmypy.json
.eggs/
.idea/
.installed.cfg
.mypy_cache/
.pytest_cache/
.ruff_cache/
.serena/
.tox/
.vscode/
ENV/
MANIFEST
__pycache__/
build/
develop-eggs/
dist/
dmypy.json
downloads/
eggs/
env.bak/
env/
htmlcov/
lib/
lib64/
# NOTE: `libs/` (plural) was retired in slice 0.2 — pyremindkit moved into
# `src/mcp_apple_reminders/_native/`. Only `lib/` and `lib64/` (Python build
# artifacts) are ignored here.

# Native helper build outputs (compiled in slice 0.6 by `make build-native`).
# Sources live in `_native/src/`; binaries land in `_native/bin/`.
src/mcp_apple_reminders/_native/bin/
local_config.json
parts/
pip-wheel-metadata/
sdist/
share/python-wheels/
tfplan
tfplan.*
var/
venv.bak/
venv/
wheels/
